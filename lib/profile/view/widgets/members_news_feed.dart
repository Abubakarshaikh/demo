import 'package:flutter/material.dart';

class MembersNewsFeed extends StatelessWidget {
  const MembersNewsFeed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
