import 'package:flutter/material.dart';

class FilesNewsFeed extends StatelessWidget {
  const FilesNewsFeed({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}